// ============================= //
// Adding Parameters to Methods  //
// ============================= //

// Add a Book parameter to the 'customerLend' and 'customerReturn' methods.
// Implement the functionality described for each method.

public class Library {

  void customerLend() {
    /* If the book is available, then:
       * Print the book's information using the `bookInfo()` method
       * Lend the book using the `lendBook()` method
       * Print a message asking the customer to return the book in two weeks

       Otherwise, print a message stating that the book is not available to borrow */
  }

  void customerReturn() {
    /* If the book is not available, then:
       * Return the book using the `returnBook()` method
       * Print a message thanking the customer for returning the book

       Otherwise, print a message stating that the book is not on loan */
  }
}
